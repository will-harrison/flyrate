---
phase: 01-foundation-submit
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - supabase/migrations/0003_taxonomy.sql
  - supabase/migrations/0004_taxonomy_seed.sql
  - supabase/migrations/0005_flies_photos_and_rls.sql
  - supabase/migrations/0006_storage_bucket.sql
  - src/types/database.types.ts
  - src/features/submit/submit-fly-schema.ts
  - src/features/submit/submit-fly-form.tsx
  - src/features/submit/submit-fly-action.ts
  - src/features/submit/photo-pipeline.ts
  - src/features/submit/taxonomy.ts
  - src/features/submit/__tests__/flies-insert-rls.integration.test.ts
  - src/app/flies/new/page.tsx
  - src/app/flies/[id]/page.tsx
  - src/app/flies/[id]/not-found.tsx
autonomous: false
requirements: [TAX-01, SUB-01, SUB-02, SUB-03, SUB-04, SUB-05, SUB-06, SUB-07]

must_haves:
  truths:
    - "The submit form's fly-type, subcategory, and target-fish choices come only from the seeded, admin-managed taxonomy lookup tables — no free-text taxonomy entry."
    - "A signed-in, email-confirmed user can submit a fly with one or more photos, a structured recipe (ordered material rows + hook size + thread), a free-text description, a difficulty level, one fly type + one subcategory, and one or more target fish."
    - "Photos are client-side compressed and stripped of EXIF/GPS before reaching storage; each fly photo is stored under the uploader's own path prefix."
    - "On successful submit the author is redirected to /flies/[id] and sees their fly rendered — the 'see it exist' payoff."
    - "An unconfirmed or non-owner user cannot insert a fly — blocked at the DB by RLS, not just the UI."
  artifacts:
    - "supabase/migrations/0003_taxonomy.sql — fly_types, fly_subcategories, fish_types lookup tables + public-read RLS"
    - "supabase/migrations/0004_taxonomy_seed.sql — seeded rows (D-05..D-07)"
    - "supabase/migrations/0005_flies_photos_and_rls.sql — flies, fly_materials, fly_fish_types, fly_photos + owner/confirmed RLS"
    - "supabase/migrations/0006_storage_bucket.sql — fly-photos bucket + storage.objects RLS"
    - "src/features/submit/* and src/app/flies/{new,[id]} — form, schema, action, pipeline, detail page"
  key_links:
    - "flies-insert RLS WITH CHECK must call is_email_confirmed() (Plan 03) AND (select auth.uid()) = author_id — the actual authorization boundary, not the Server Action."
    - "storage.objects insert RLS restricts writes to (storage.foldername(name))[1] = (select auth.uid())::text — path-prefix ownership."
    - "browser-image-compression with preserveExif:false is the SUB-07 EXIF/GPS-strip mechanism; never set preserveExif:true."
    - "each fly_subcategory belongs to exactly one fly_type_id (D-06 Terrestrial is its own top-level type, not a Dry Fly subcategory)."
---

<objective>
Deliver the phase's core payoff as one vertical slice: seed the admin-managed taxonomy, create the flies/photos/materials/junction schema with owner-and-confirmed RLS and the `fly-photos` storage bucket, apply everything to the cloud DB ([BLOCKING] schema push), then build the single-scroll submit form (react-hook-form + zod + `useFieldArray`) with the client-side compress+EXIF-strip photo pipeline and a Server Action that re-validates and inserts, redirecting the author to `/flies/[id]` where they see their fly exist.

Purpose: This closes the "submit → see it exist" user story that is the whole point of Phase 1. It depends on Plan 03's `is_email_confirmed()` gate (authored as migration 0002 in Plan 03 and first applied here in Task 2's consolidated push) and Plan 02's clients/types.

Output: Migrations 0002-0006 applied to the cloud DB via one consolidated push; the submit form + schema + action + photo pipeline; the taxonomy fetch; and the author-visible `/flies/[id]` detail page.

AUTONOMY NOTE: this plan's `autonomous: false` flag applies SPECIFICALLY to Task 2 — the `[BLOCKING] supabase db push`, which may require human escalation if the Supabase CLI/token/link is unavailable (see Task 2's escalation clause). Tasks 1, 3, 4, and 5 run autonomously (migration authoring, schema/action/pipeline code, and the detail page — all verifiable by `npm run build`/grep without a live DB); Task 6 is the end-of-slice `checkpoint:human-verify`. The plan is marked non-autonomous solely because Task 2's push and Task 6's verification need a human when the CLI/token is out of reach.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-foundation-submit/01-CONTEXT.md
@.planning/phases/01-foundation-submit/01-RESEARCH.md
@.planning/phases/01-foundation-submit/01-UI-SPEC.md
@.planning/phases/01-foundation-submit/01-03-SUMMARY.md

<interfaces>
<!-- Consumes Plan 02 clients/types and Plan 03's is_email_confirmed(); defines the flies schema Phases 2-4 read. -->

Consumes:
  src/lib/supabase/client.ts -> createClient()   (browser — photo upload from the form)
  src/lib/supabase/server.ts -> createClient()   (server — Server Action insert + detail-page fetch)
  DB public.is_email_confirmed() returns boolean  (Plan 03 — called in flies-insert WITH CHECK)

Creates (schema consumed by Phases 2-4):
  fly_types(id uuid pk, name text unique, slug text unique, position int)
  fly_subcategories(id uuid pk, fly_type_id uuid fk -> fly_types, name text, slug text, position int)   -- unique(fly_type_id, name)
  fish_types(id uuid pk, name text unique, slug text unique, position int)
  flies(id uuid pk, author_id uuid fk -> auth.users, name text, description text,
        hook_size text, thread text, difficulty text check in (beginner,intermediate,advanced),
        fly_type_id fk, fly_subcategory_id fk, primary_photo_id uuid, created_at timestamptz)
  fly_materials(id uuid pk, fly_id uuid fk -> flies on delete cascade, position int, label text, material text, color text)
  fly_fish_types(fly_id uuid fk, fish_type_id uuid fk, primary key(fly_id, fish_type_id))
  fly_photos(id uuid pk, fly_id uuid fk -> flies on delete cascade, storage_path text, position int)

  src/features/submit/submit-fly-schema.ts:
    export const submitFlySchema: z.ZodType
    export type SubmitFlyInput = z.infer<typeof submitFlySchema>
</interfaces>
</context>

<artifacts_this_phase_produces>
This plan produces:
- Files: `supabase/migrations/0003_taxonomy.sql`, `supabase/migrations/0004_taxonomy_seed.sql`, `supabase/migrations/0005_flies_photos_and_rls.sql`, `supabase/migrations/0006_storage_bucket.sql`, regenerated `src/types/database.types.ts`, `src/features/submit/submit-fly-schema.ts`, `src/features/submit/submit-fly-form.tsx`, `src/features/submit/submit-fly-action.ts`, `src/features/submit/photo-pipeline.ts`, `src/features/submit/taxonomy.ts`, `src/app/flies/new/page.tsx`, `src/app/flies/[id]/page.tsx`, `src/app/flies/[id]/not-found.tsx`.
- DB objects: tables `fly_types`, `fly_subcategories`, `fish_types`, `flies`, `fly_materials`, `fly_fish_types`, `fly_photos`; RLS policies on each (public read; owner+confirmed insert on flies; owner writes on children); storage bucket `fly-photos` + `storage.objects` RLS (public read, owner-prefix write); seeded taxonomy rows.
- Routes: `/flies/new` (submit form), `/flies/[id]` (author-visible detail page, D-02 payoff).
- Server Action: `submitFlyAction`. Zod schema: `submitFlySchema`.

MIGRATION NUMBERING LOCK (for downstream phases): Phase 1 owns migrations `0001` through `0006` (0001 profiles/bootstrap; 0002 is_email_confirmed helper; 0003 taxonomy; 0004 taxonomy seed; 0005 flies/photos/RLS; 0006 storage bucket). These numbers are LOCKED to Phase 1. Any downstream phase (Phase 2+) MUST start its migrations at `0007_*` and continue sequentially — do NOT renumber, reuse, or interleave with 0001-0006. `supabase db push` applies migrations in filename order, so reusing an existing number would silently skip or misorder a migration.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Taxonomy + flies/photos schema, seed, and storage bucket migrations (0003-0006)</name>
  <files>supabase/migrations/0003_taxonomy.sql, supabase/migrations/0004_taxonomy_seed.sql, supabase/migrations/0005_flies_photos_and_rls.sql, supabase/migrations/0006_storage_bucket.sql</files>
  <read_first>
    - .claude/CLAUDE.md (Data Model & RLS Patterns — lookup tables NOT enums; RLS on every table in its creating migration; anon read USING(true), authenticated owner writes; Image handling — bucket fly-photos, path {user_id}/{fly_id}/{uuid}.webp)
    - .planning/phases/01-foundation-submit/01-CONTEXT.md (D-03 no dedup; D-04 multi-photo + primary; D-05 species-level fish; D-06 six fly types incl. Terrestrial top-level; D-07 lean seed + Specific Ideas seed list)
    - .planning/phases/01-foundation-submit/01-RESEARCH.md (Pitfall 3 — each subcategory under exactly one fly_type; Pattern 6 — is_email_confirmed gate; Code Examples — storage RLS owner-prefix; State of the Art — wrap (select auth.uid()))
    - .planning/research/ARCHITECTURE.md (schema + RLS sketch)
  </read_first>
  <behavior>
    - Selecting from fly_types returns exactly 6 rows: Dry Fly, Nymph, Streamer, Wet Fly, Emerger, Terrestrial (D-06).
    - Every fly_subcategories row references exactly one fly_type_id; no subcategory name is duplicated across two fly_types (D-06/Pitfall 3 — e.g. Ant/Beetle/Hopper appear only under Terrestrial, never under Dry Fly).
    - fish_types contains the ~12 species-level rows from CONTEXT (Rainbow/Brown/Brook/Cutthroat Trout as separate rows — D-05).
    - anon and authenticated can SELECT all taxonomy tables, flies, fly_materials, fly_fish_types, fly_photos.
    - Inserting a fly succeeds only when the caller is authenticated, is the author (author_id = uid), and is email-confirmed; an unconfirmed or non-owner insert is rejected by RLS.
    - Inserting a storage object under another user's uid prefix is rejected; under the caller's own uid prefix it succeeds.
    - RLS is enabled on every new table within its own creating migration.
  </behavior>
  <action>Author four migrations. `0003_taxonomy.sql`: create `fly_types`, `fly_subcategories` (FK `fly_type_id` + `unique(fly_type_id, name)`), `fish_types` as lookup TABLES (never Postgres enums), enable RLS in-file, add public-read SELECT policies for `anon, authenticated` using `true` (no client insert path this phase — TAX-01 admin-managed). `0004_taxonomy_seed.sql`: insert the D-05..D-07 seed rows exactly per the CONTEXT Specific Ideas list — 6 fly types, their curated subcategories (Ant/Beetle/Hopper ONLY under Terrestrial per Pitfall 3), and the 12 species-level fish. `0005_flies_photos_and_rls.sql`: create `flies` (scalar `hook_size`, `thread`, `description`, `difficulty text check (difficulty in ('beginner','intermediate','advanced'))`, `author_id`, `fly_type_id`, `fly_subcategory_id`, `primary_photo_id`, `created_at`), `fly_materials` (ordered child, `position int`, free-text `label`/`material`/`color` — NOT a materials lookup table, per RESEARCH), `fly_fish_types` junction (SUB-06 many-to-many), `fly_photos` (D-04 multi-photo); enable RLS in-file; public-read SELECT on all four; flies INSERT policy `with check ((select auth.uid()) = author_id and public.is_email_confirmed())` (AUTH-02 + ownership); child-table (materials/fish/photos) writes gated on ownership of the parent fly via `(select auth.uid())`; no uniqueness constraint on fly name/design (D-03). Wrap every `auth.uid()` as `(select auth.uid())`. `0006_storage_bucket.sql`: create the public `fly-photos` bucket; `storage.objects` SELECT policy public read for `bucket_id='fly-photos'`; INSERT/UPDATE policy `with check (bucket_id='fly-photos' and (storage.foldername(name))[1] = (select auth.uid())::text)` (owner-prefix write). Do NOT apply here — the push is the [BLOCKING] Task 2.</action>
  <verify>
    <automated>for f in 0003_taxonomy 0004_taxonomy_seed 0005_flies_photos_and_rls 0006_storage_bucket; do test -f "supabase/migrations/$f.sql" || { echo "missing $f"; exit 1; }; done; grep -iq 'enable row level security' supabase/migrations/0003_taxonomy.sql && grep -iq 'enable row level security' supabase/migrations/0005_flies_photos_and_rls.sql && grep -iq 'is_email_confirmed' supabase/migrations/0005_flies_photos_and_rls.sql && grep -iq 'storage.foldername' supabase/migrations/0006_storage_bucket.sql</automated>
  </verify>
  <acceptance_criteria>
    - All four migration files exist.
    - `0003_taxonomy.sql` and `0005_flies_photos_and_rls.sql` each contain `enable row level security` (RLS in the creating migration for every table); no Postgres `enum` type is created for taxonomy (grep finds no `create type ... as enum`).
    - `0005_flies_photos_and_rls.sql` flies-insert policy WITH CHECK references BOTH `(select auth.uid()) = author_id` AND `is_email_confirmed()` (AUTH-02 + ownership at the DB).
    - `0006_storage_bucket.sql` insert/update policy uses `(storage.foldername(name))[1] = (select auth.uid())::text` (owner-prefix write).
    - Seed file inserts 6 fly types and 12 fish types; Ant/Beetle/Hopper subcategories reference the Terrestrial fly_type only (Pitfall 3 / D-06).
    - No `flies` uniqueness constraint on name/design (D-03).
    - Every `auth.uid()` occurrence is wrapped as `(select auth.uid())`.
  </acceptance_criteria>
  <done>Migrations 0003-0006 author the taxonomy (lookup tables + seed), flies/photos/materials/junction schema with owner+confirmed RLS, and the fly-photos storage bucket with owner-prefix RLS — all with RLS in-file. Not yet applied (Task 2 applies).</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Consolidated push of migrations 0002-0006 to the cloud DB and regenerate types</name>
  <files>src/types/database.types.ts</files>
  <read_first>
    - .planning/phases/01-foundation-submit/01-RESEARCH.md (Environment Availability — Docker daemon NOT running / Supabase CLI NOT installed; non-TTY push needs SUPABASE_ACCESS_TOKEN; recommend cloud push over local `supabase start`)
    - .planning/phases/01-foundation-submit/01-01-SUMMARY.md (project ref + access token availability)
    - .planning/phases/01-foundation-submit/01-03-SUMMARY.md (migration 0002 is AUTHORED in Plan 03 but applied HERE — this task is its first apply)
    - supabase/migrations/0002_email_confirmed_helper.sql (Plan 03 — authored there, applied here)
    - supabase/migrations/0003_taxonomy.sql, 0004_taxonomy_seed.sql, 0005_flies_photos_and_rls.sql, 0006_storage_bucket.sql (Task 1)
  </read_first>
  <action>[BLOCKING — the phase cannot pass verification until the schema is live; build/type checks pass without this push and would produce a false-positive.] Ensure the repo is linked to the cloud project (`supabase link --project-ref <ref>` using `SUPABASE_ACCESS_TOKEN` for non-TTY auth) and run `supabase db push`. This is the CONSOLIDATED push for the phase: it applies every pending migration — explicitly `0001`, `0002`, `0003`, `0004`, `0005`, and `0006`. Migration `0001` (Plan 02) was already applied in Plan 02; migration `0002_email_confirmed_helper.sql` (Plan 03) was AUTHORED in Plan 03 but deliberately NOT pushed there — this task is where `0002` first goes live, alongside `0003-0006`. A `supabase db push` is idempotent for already-applied migrations, so `0001` is skipped and `0002-0006` are applied. Because Plan 04's flies-insert RLS `WITH CHECK` in `0005` calls `is_email_confirmed()` (defined in `0002`), `0002` MUST be applied in the same push before/with `0005`; `supabase db push` applies migrations in filename order (0002 → 0005), so a single ordered push satisfies this dependency. Then regenerate `src/types/database.types.ts` via `supabase gen types typescript --linked`. NOTE: Docker is NOT required for a cloud `db push`. ESCALATION ON BLOCKED PUSH: if the Supabase CLI is not installed, `SUPABASE_ACCESS_TOKEN` is missing/invalid, the link fails, or the push otherwise cannot run, do NOT fake success and do NOT proceed past this task — emit a clear blocked signal to /gsd-execute-phase (status `blocked-external`) naming the specific missing prerequisite (CLI / token / link / network) and the exact command that could not run, and halt. Docker being unavailable is NOT a valid reason to block a cloud push (cloud `db push` needs no Docker); only genuine CLI/token/link/network unavailability blocks. After a successful push, run the Supabase Security Advisor (or the dashboard linter) and confirm no "RLS disabled" findings on any public table.</action>
  <verify>
    <automated>grep -q 'flies' src/types/database.types.ts && grep -q 'fly_types' src/types/database.types.ts && grep -q 'fish_types' src/types/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` reports migrations `0002` (if not already applied) and `0003-0006` applied to the cloud project (or the human confirms the apply if escalated). `is_email_confirmed()` from `0002` MUST be live before/with `0005` (its flies-insert WITH CHECK calls it); confirm the function exists in the cloud DB after the push.
    - If the push could not run (missing CLI / token / link / network), the task emitted a `blocked-external` signal to /gsd-execute-phase naming the exact missing prerequisite and did NOT proceed or fake success.
    - Regenerated `database.types.ts` includes `flies`, `fly_types`, `fly_subcategories`, `fish_types`, `fly_materials`, `fly_fish_types`, `fly_photos`.
    - Supabase Security Advisor shows no "RLS disabled" finding on any public table (mitigates the CVE-2025-48757 class).
    - The `fly-photos` bucket exists in the cloud project's Storage.
  </acceptance_criteria>
  <done>All Phase 1 migrations (0001-0006, with 0002's `is_email_confirmed()` applied in this consolidated push before 0005 relies on it) are live on the cloud DB, types are regenerated to match, and the Security Advisor confirms RLS is enabled on every public table. If the push was blocked by a missing prerequisite, a `blocked-external` signal was emitted and the task halted rather than reporting success.</done>
</task>

<task type="auto">
  <name>Task 3: Submit-fly zod schema, taxonomy fetch, and client photo pipeline</name>
  <files>src/features/submit/submit-fly-schema.ts, src/features/submit/taxonomy.ts, src/features/submit/photo-pipeline.ts</files>
  <read_first>
    - .planning/phases/01-foundation-submit/01-RESEARCH.md (Pattern 7 — submit-fly-schema shape incl. materials array, fishTypeIds min 1, photoPaths, primaryPhotoIndex; Pattern 8 — photo pipeline maxSizeMB/maxWidthOrHeight/webp, preserveExif MUST stay false; A4 orientation caveat)
    - .claude/CLAUDE.md (Image handling — path {user_id}/{fly_id}/{uuid}.webp; client-side compression free tier)
    - src/types/database.types.ts (Task 2 regenerated types), src/lib/supabase/server.ts (taxonomy fetch)
  </read_first>
  <action>Create `submit-fly-schema.ts` exporting `submitFlySchema` (zod v4) and `SubmitFlyInput` per RESEARCH Pattern 7: `name`, optional `description` (max 2000), `hookSize`, `thread`, `difficulty` enum(beginner/intermediate/advanced) (SUB-04), `flyTypeId`/`flySubcategoryId` uuids (SUB-05), `fishTypeIds` uuid array min 1 (SUB-06), `materials` array min 1 of {label, material, optional color} (SUB-02 ordered rows), `photoPaths` array min 1 max 6 (SUB-01 + D-04 photo cap 6), `primaryPhotoIndex` int min 0 (D-04 user-selected cover). Create `taxonomy.ts` with a server-side fetch (server client) returning the seeded fly_types, fly_subcategories (grouped by fly_type_id for the cascade), and fish_types for the form (TAX-01 — options come only from the lookup tables). Create `photo-pipeline.ts` exporting a per-file `prepareForUpload(file)` using `browser-image-compression` with `maxSizeMB:1`, `maxWidthOrHeight:1920`, `useWebWorker:true`, `fileType:'image/webp'`, and preserveExif left at its default false (SUB-07 EXIF/GPS strip) — this literal must never be set true — plus an `uploadPhoto(userId, flyId, file)` that builds the path `${userId}/${flyId}/${crypto.randomUUID()}.webp` and uploads to the `fly-photos` bucket via the browser client.</action>
  <verify>
    <automated>npm run build && grep -q "browser-image-compression" src/features/submit/photo-pipeline.ts && grep -q "image/webp" src/features/submit/photo-pipeline.ts && grep -q "fishTypeIds" src/features/submit/submit-fly-schema.ts && grep -q "primaryPhotoIndex" src/features/submit/submit-fly-schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `submit-fly-schema.ts` validates: >=1 material row, >=1 fish type, 1..6 photos, a difficulty enum, fly type + subcategory uuids, and a primary photo index (SUB-02..06 + D-04).
    - `taxonomy.ts` fetches options from `fly_types`/`fly_subcategories`/`fish_types` only (TAX-01 — no free-text taxonomy).
    - `photo-pipeline.ts` uses `browser-image-compression` to webp and does NOT set `preserveExif: true` (SUB-07); upload path is `{userId}/{flyId}/{uuid}.webp`.
  </acceptance_criteria>
  <done>The shared zod schema, taxonomy fetch, and the compress+EXIF-strip+upload pipeline exist and build; taxonomy options are sourced only from the lookup tables.</done>
</task>

<task type="auto">
  <name>Task 4: Single-scroll submit form + Server Action insert (redirect to /flies/[id]) + automated unconfirmed-insert RLS-rejection test</name>
  <files>src/app/flies/new/page.tsx, src/features/submit/submit-fly-form.tsx, src/features/submit/submit-fly-action.ts, src/features/submit/__tests__/flies-insert-rls.integration.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation-submit/01-CONTEXT.md (D-01 single scrollable page not a wizard; D-02 redirect to /flies/[id]; D-04 primary photo select)
    - .planning/phases/01-foundation-submit/01-UI-SPEC.md (Submit form component inventory — Card per section, useFieldArray material rows keyed by field.id, cascade Select, ToggleGroup fish multi-select, RadioGroup difficulty, Textarea, sticky Submit disabled while unconfirmed; Copywriting — "Submit Fly", empty states, unconfirmed-email Alert)
    - .planning/phases/01-foundation-submit/01-RESEARCH.md (Pattern 7 useFieldArray keyed by field.id — Pitfall 4; Architecture — Server Action re-validates zod server-side; is_email_confirmed gate is the real boundary)
    - src/features/submit/submit-fly-schema.ts, src/features/submit/photo-pipeline.ts, src/features/submit/taxonomy.ts (Task 3), src/lib/supabase/server.ts
  </read_first>
  <action>Create `/flies/new/page.tsx` as a Server Component that (a) redirects unauthenticated users to `/auth/sign-in?next=/flies/new`, (b) fetches taxonomy via Task 3's `taxonomy.ts`, and (c) renders the client `submit-fly-form.tsx`. Build `submit-fly-form.tsx` (client) as a SINGLE scrollable page (D-01) with a `Card` per section — Photos, Recipe (hook size + thread + `useFieldArray` ordered material rows keyed by `field.id` per Pitfall 4, with up/down reorder), Taxonomy (fly type -> subcategory cascade Select + fish `ToggleGroup type="multiple"`), Difficulty (`RadioGroup`), Description (`Textarea`) — matching the UI-SPEC. On submit: compress+strip+upload each photo via the pipeline (showing per-file progress, respecting the 6-photo cap and user-selected cover = `primaryPhotoIndex`), then call the Server Action with the validated payload + uploaded `photoPaths`. Disable the "Submit Fly" button and show the "Confirm your email to submit a fly" Alert when the session's `email_confirmed_at` is null (belt-and-suspenders; RLS is the real gate). Create `submit-fly-action.ts` (`'use server'`) that re-parses the payload with `submitFlySchema` server-side (never trust the client), inserts the `flies` row (author_id = uid) then `fly_materials` (ordered by position), `fly_fish_types`, and `fly_photos`, sets `primary_photo_id` from the chosen cover, and `redirect(`/flies/${id}?submitted=1`)` (D-02). The RLS `WITH CHECK` from Task 1 is the authorization boundary; the action is a convenience+validation layer. AUTOMATED AUTH-02 DB-GATE TEST (do not rely solely on the Task 6 human-verify for this): create `src/features/submit/__tests__/flies-insert-rls.integration.test.ts` (Vitest) that programmatically signs up an UNCONFIRMED user against the cloud project (email/password, no email confirmation performed) using the anon-key Supabase client, obtains that unconfirmed session, and attempts a direct `insert` into `public.flies` with `author_id` set to that user's uid. Assert the insert is REJECTED by RLS (an error is returned and no row is created) — proving the `0005` flies-insert `WITH CHECK (... and public.is_email_confirmed())` gate blocks unconfirmed writers at the DB, not just in the UI. Guard the test so it is skipped with a clear message when `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` are absent (so it does not fail in a credential-less CI), but it MUST run and pass in the developer/verification environment where those env vars are set and migrations 0002-0006 are live. Clean up the test user/rows in an afterAll where possible.</action>
  <verify>
    <automated>npm run build && grep -q "useFieldArray" src/features/submit/submit-fly-form.tsx && grep -q "field.id" src/features/submit/submit-fly-form.tsx && grep -q "use server" src/features/submit/submit-fly-action.ts && grep -q "submitFlySchema" src/features/submit/submit-fly-action.ts && grep -q "redirect" src/features/submit/submit-fly-action.ts && test -f src/features/submit/__tests__/flies-insert-rls.integration.test.ts && npx vitest run src/features/submit/__tests__/flies-insert-rls.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with `/flies/new` present.
    - The form is a single scrollable page (D-01), one Card per section; material rows use `useFieldArray` keyed by `field.id` (Pitfall 4), not array index.
    - Taxonomy uses a fly-type -> subcategory cascade and a multi-select for fish (>=1); difficulty is a RadioGroup (SUB-04); description is a Textarea (SUB-03).
    - Photos upload through the compress+strip pipeline with a user-selected cover; 6-photo cap enforced (D-04).
    - The Submit button is disabled + an Alert shown when `email_confirmed_at` is null (AUTH-02 UI belt; RLS is the real gate).
    - `submit-fly-action.ts` is `'use server'`, re-parses with `submitFlySchema` server-side, inserts flies + materials + fish + photos, and `redirect`s to `/flies/${id}?submitted=1` (D-02).
    - The Vitest integration test `flies-insert-rls.integration.test.ts` asserts an UNCONFIRMED user's direct `flies` INSERT is RLS-rejected (AUTH-02 DB gate proven automatically, not only at the Task 6 human-verify). The test runs and passes when the Supabase env vars are set and migrations 0002-0006 are live; it skips with a clear message when those env vars are absent.
  </acceptance_criteria>
  <done>A signed-in, confirmed user fills the single-scroll form and submits; the Server Action re-validates and inserts the fly + its children; the user is redirected to the fly's own detail page. An automated Vitest integration test independently proves the AUTH-02 gate: an unconfirmed user's flies INSERT is rejected by RLS at the DB.</done>
</task>
<task type="auto">
  <name>Task 5: Author-visible /flies/[id] detail page (the "see it exist" payoff)</name>
  <files>src/app/flies/[id]/page.tsx, src/app/flies/[id]/not-found.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-submit/01-CONTEXT.md (D-02 detail page renders everything entered; author-visible, seed of Phase 2 public page)
    - .planning/phases/01-foundation-submit/01-UI-SPEC.md (Fly detail — primary photo large + gallery via AspectRatio; Badge chips for type/subcategory/fish/difficulty; recipe as Table/definition-list; read-only star stub "No ratings yet"; success banner "Your fly is live." on ?submitted=1)
    - src/lib/supabase/server.ts, src/types/database.types.ts
  </read_first>
  <action>Create `/flies/[id]/page.tsx` as a Server Component that fetches the fly by id with the server client — joining `fly_photos` (ordered, primary first via `primary_photo_id`), `fly_materials` (ordered by position), `fly_fish_types` -> `fish_types`, and the fly_type/subcategory names — and renders: the fly name (Display), the primary photo large + remaining photos in a gallery (shadcn `AspectRatio`), `Badge` chips for fly type / subcategory / each target fish / difficulty, the recipe as a definition-list/Table (hook size, thread, then ordered material rows), the description as plain body text (React auto-escaped — never `dangerouslySetInnerHTML`), and a read-only star stub with "No ratings yet" (rating is Phase 3). When `?submitted=1` is present, show the dismissible "Your fly is live." success banner (UI-SPEC Copywriting). Photo `src` uses `getPublicUrl` on the stored path. Create `not-found.tsx` and call `notFound()` if the id does not resolve.</action>
  <verify>
    <automated>npm run build && grep -q "notFound" src/app/flies/[id]/page.tsx && grep -q "getPublicUrl" src/app/flies/[id]/page.tsx && ! grep -rq "dangerouslySetInnerHTML" src/app/flies/</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with `/flies/[id]` present.
    - The page server-fetches the fly + photos + materials + fish + taxonomy names and renders them (D-02 — everything entered is visible).
    - Primary photo leads the gallery; recipe shows hook size, thread, and ordered material rows; type/subcategory/fish/difficulty render as chips.
    - Description is rendered as escaped text; no `dangerouslySetInnerHTML` anywhere under `src/app/flies/` (stored-XSS mitigation).
    - `?submitted=1` shows the "Your fly is live." banner; an unknown id renders the not-found page.
  </acceptance_criteria>
  <done>Immediately after submit the author lands on `/flies/[id]` and sees their fully-cataloged fly rendered — the phase's "see it exist" payoff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Verify the end-to-end submit -> see-it-exist slice against the cloud DB</name>
  <files>(verification only — no repo files changed)</files>
  <action>Human verification: as a confirmed user, submit a fully-cataloged fly via `/flies/new` (including a real portrait-mode photo and a non-first cover), confirm redirect to `/flies/[id]` with everything rendered and correct orientation, confirm an uploaded Storage object is webp with no EXIF/GPS, and confirm an unconfirmed user's insert is DB-rejected. See <how-to-verify> for exact steps.</action>
  <read_first>
    - .planning/phases/01-foundation-submit/01-SKELETON.md (Capability Proven End-to-End)
    - .planning/phases/01-foundation-submit/01-RESEARCH.md (A4 orientation caveat — test one real portrait phone photo; Security Domain — EXIF/GPS strip)
    - .planning/phases/01-foundation-submit/01-UI-SPEC.md (Key States — submit form + detail page)
  </read_first>
  <what-built>The running app with seeded taxonomy, the single-scroll submit form, the compress+EXIF-strip photo pipeline, the Server Action insert, and the author-visible detail page — all against the cloud Supabase project.</what-built>
  <how-to-verify>
    1. `npm run dev`; sign in as a CONFIRMED user and visit `/flies/new`.
    2. Confirm the fly-type dropdown lists the 6 seeded types and subcategory/fish options come only from the seed (TAX-01).
    3. Add >=2 photos (include one real portrait-mode phone photo), pick a non-first cover, add >=2 ordered material rows, set hook size + thread, choose a difficulty, pick a fly type + subcategory + >=1 fish, write a description; submit.
    4. Confirm redirect to `/flies/[id]` with the "Your fly is live." banner and everything you entered rendered; the portrait photo is NOT sideways (A4 orientation check) and the chosen cover leads (D-04).
    5. In Supabase Storage, open one uploaded object and confirm it is webp and carries NO GPS/EXIF metadata (SUB-07).
    6. Sign in as an UNCONFIRMED user (or a second account before confirming) and confirm the Submit button is disabled with the "Confirm your email to submit a fly" Alert; attempt a direct insert and confirm RLS rejects it (AUTH-02 DB gate).
  </how-to-verify>
  <acceptance_criteria>
    - Taxonomy options come only from the seeded lookup tables (TAX-01).
    - A fly submits with >=1 photo, ordered recipe rows + hook size + thread, description, difficulty, one fly type + subcategory, >=1 fish (SUB-01..06) and is visible to its author on `/flies/[id]` (D-02).
    - Uploaded photos are webp and stripped of EXIF/GPS; portrait orientation is correct (SUB-07 + A4).
    - An unconfirmed user cannot insert a fly — blocked at the DB by RLS (AUTH-02).
  </acceptance_criteria>
  <resume-signal>Type "approved" once a confirmed user can submit a fully-cataloged fly and see it on `/flies/[id]`, photos are EXIF-stripped webp with correct orientation, and an unconfirmed user's insert is DB-rejected — or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Storage upload | Client uploads photo objects; path prefix determines ownership. |
| browser/client -> Server Action | Untrusted submit payload; must be re-validated server-side. |
| authenticated (maybe unconfirmed) user -> flies insert | Only confirmed owners may write; enforced in RLS. |
| stored fly fields -> detail page render | Free-text description/recipe rendered back to the author. |
| phone photo EXIF -> Storage | GPS/location metadata must not leave the device. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | Missing/late RLS on flies/materials/fish/photos exposing writes via the anon key | high | mitigate | Task 1 enables RLS in each creating migration; public-read SELECT + owner writes; Task 2 confirms via Security Advisor (no "RLS disabled" finding). |
| T-04-02 | Spoofing / Repudiation | Unconfirmed/spam user inserting flies | high | mitigate | flies-insert WITH CHECK calls `is_email_confirmed()` (Plan 03's migration 0002, applied in Task 2's consolidated push) AND `(select auth.uid()) = author_id` (Task 1) — DB gate; UI Alert is belt-and-suspenders (Task 4). Proven by an automated Vitest integration test (Task 4) that an unconfirmed user's INSERT is RLS-rejected, and re-checked at the Task 6 human-verify. |
| T-04-03 | Tampering / Elevation of Privilege | Uploading photos outside the user's own path prefix | high | mitigate | `storage.objects` insert policy `(storage.foldername(name))[1] = (select auth.uid())::text` (Task 1); path built as `{userId}/{flyId}/{uuid}.webp` (Task 3). |
| T-04-04 | Information Disclosure | EXIF/GPS location metadata leaking a user's tying spot | high | mitigate | `browser-image-compression` canvas re-encode with `preserveExif:false` strips EXIF/GPS before upload (Task 3, SUB-07); verified on a real photo at Task 6. |
| T-04-05 | Tampering | Stored XSS via free-text description/recipe on the detail page | medium | mitigate | React escapes text by default; Task 5 forbids `dangerouslySetInnerHTML` under `src/app/flies/` (grep-asserted). |
| T-04-06 | Tampering | Client bypasses form validation and posts a malformed/oversized payload | medium | mitigate | Server Action re-parses with `submitFlySchema` server-side (Task 4) — the client zod validation is UX only; DB constraints (difficulty check, FKs, NOT NULLs) are the final guard. |
| T-04-07 | Elevation of Privilege | Client submits a `fly_type`/`fish` id not in the taxonomy | medium | mitigate | FKs to the lookup tables reject unknown ids; taxonomy options are fetched only from the seeded tables (Task 3, TAX-01). |
| T-04-SC | Tampering | npm/pip/cargo installs | high | accept | No new package installs in this plan (uses Plan 02's dependency set: `browser-image-compression`, `react-hook-form`, `zod`, `@hookform/resolvers`). Legitimacy already handled in Plan 02 per RESEARCH.md `## Package Legitimacy Audit`. |
</threat_model>

<verification>
- Migrations 0002-0006 are applied to the cloud DB via Task 2's consolidated push (0002's `is_email_confirmed()` live before 0005's policy relies on it); 0003-0006 exist with RLS in-file; types regenerated include the flies schema; Security Advisor shows no RLS-disabled findings.
- `npm run build` passes with `/flies/new` and `/flies/[id]`.
- Taxonomy options come only from the seeded lookup tables (TAX-01).
- Automated: `flies-insert-rls.integration.test.ts` (Vitest) asserts an unconfirmed user's flies INSERT is RLS-rejected (AUTH-02 DB gate), running against the live cloud DB when env vars are set.
- Manual (Task 6): a confirmed user submits a full fly and sees it on `/flies/[id]`; photos are webp + EXIF/GPS-stripped with correct orientation; an unconfirmed user's insert is DB-rejected.
- Migration numbering lock recorded: Phase 1 owns 0001-0006; downstream phases start at 0007_*.
</verification>

<success_criteria>
The admin-managed taxonomy is seeded and gates the submit form (TAX-01); a signed-in, email-confirmed user can submit a fly with >=1 photo (SUB-01), an ordered recipe + hook size + thread (SUB-02), a free-text description (SUB-03), a difficulty (SUB-04), one fly type + subcategory (SUB-05), and >=1 target fish (SUB-06); photos are compressed + EXIF/GPS-stripped before upload and stored under the owner's prefix (SUB-07); and the author is redirected to `/flies/[id]` and sees the fly exist (D-02) — the phase payoff. Unconfirmed/non-owner writes are blocked at the DB by RLS.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-submit/01-04-SUMMARY.md` when done. Record: migrations 0002-0006 applied in the consolidated push (note 0002 first went live here), migrations 0003-0006 objects + seed counts, the storage bucket + owner-prefix RLS, the submit-fly schema/action/pipeline interfaces, the `/flies/[id]` render, the automated unconfirmed-insert RLS rejection test (Task 4), and the verified end-to-end submit -> see-it-exist slice. Also record the MIGRATION NUMBERING LOCK: Phase 1 owns 0001-0006; downstream phases start at 0007_*.
</output>